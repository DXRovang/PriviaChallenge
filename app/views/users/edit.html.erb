<h1 class="title">Update Client Form</h1>

<%= form_for @user do |u| %>

<%= render partial: "users", locals: { u: u, user: @user } %>

<div class="margin"><%= u.submit "Update Client"%></div> 
<% end %>
<button class="margin"><%= link_to "Home", users_path() %></button>
